# 毕业设计答辩发言稿与答辩问题准备

## 一、答辩基本信息

- **论文题目**：基于Qt的番茄计时与记账日记系统设计与实现
- **项目定位**：面向个人效率管理与信息记录的桌面端综合工具
- **核心技术**：C++、Qt Widgets、JSON本地存储、QTimer、QPainter、自定义图表、OCR服务抽象
- **核心模块**：番茄计时、正向计时、日记记录、记账管理、统计分析、主题切换、图片处理与文字识别

---

## 二、正式答辩发言稿

### 2.1 答辩开场白

各位老师好，我是计算机科学与技术专业的学生。我的毕业设计题目是**《基于Qt的番茄计时与记账日记系统设计与实现》**。接下来我将从**选题背景、系统设计、关键实现、创新点与不足**几个方面，对我的毕业设计进行汇报，恳请各位老师批评指正。

---

### 2.2 研究背景与选题意义

随着数字化学习和办公场景的普及，个人用户对**时间管理、日常记录、财务管理以及图片文字提取**的需求越来越强烈。但现实中，大多数工具往往只解决其中一个问题，比如番茄钟软件只负责倒计时，记账软件只负责收支录入，日记软件只负责文本记录，图片文字识别工具又是单独的一类应用。这种功能分散会导致用户在多个软件之间频繁切换，不利于提升效率，也不利于统一管理个人数据。

因此，我希望设计一款**轻量化、集成化、本地化**的桌面系统，将番茄计时、正向计时、日记记录、记账管理、数据统计以及图片文字识别整合到同一个平台中，帮助用户在一个系统内完成多类常见操作。该课题不仅具有一定的实用价值，也能够较好体现桌面应用开发中的界面设计、数据组织、模块协作、交互优化与系统扩展能力。

---

### 2.3 系统目标与总体功能

本系统的设计目标主要有以下几点：

1. 构建一个基于Qt的桌面端综合效率管理系统；
2. 实现番茄计时、正向计时、日记记录和记账管理等核心功能；
3. 提供直观的数据统计展示，帮助用户分析专注情况和消费情况；
4. 实现日间与夜间主题切换，提升使用体验；
5. 在图片处理页面中增加文字识别能力，并为后续接入**百度飞桨文字识别API**预留统一接口；
6. 采用本地化存储方案，降低部署复杂度，保证项目演示的可控性。

从当前实现来看，系统已经包含以下主要功能模块：

- 用户登录与本地会话管理
- 番茄计时与正向计时
- 日记记录与按日期存储
- 记账录入、编辑、删除
- 统计分析与图表展示
- 日夜间主题切换
- 图片透视调整与文字识别

---

### 2.4 系统总体架构

在整体架构上，本系统采用了较为清晰的模块化设计思路。

系统启动后，首先进入登录窗口。用户登录成功后，进入主窗口。主窗口基于 `QMainWindow` 和 `QTabWidget` 组织多个功能页面，包括：

- 番茄钟页面
- 正向计时页面
- 数据统计页面
- 记账页面
- 日记页面
- 图片处理页面
- 设置与同步页面

从职责划分来看，系统大体分为三层：

- **界面层**：负责页面展示、用户输入和交互反馈；
- **服务层**：负责数据读写、统计汇总和OCR识别调用；
- **会话层**：负责当前用户登录状态的维护。

这种结构的优点在于：

- 降低页面逻辑与底层数据处理之间的耦合；
- 提高后续维护和功能扩展的便利性；
- 使系统更适合毕业设计展示和模块化讲解。

---

### 2.5 核心功能实现

#### 2.5.1 番茄计时与正向计时模块

在计时模块中，我主要使用了 Qt 的 `QTimer` 实现计时逻辑。

番茄计时模块支持：

- 输入任务名称
- 设置番茄数
- 设置专注时长和休息时长
- 开始、暂停、继续、提前结束

系统会根据当前状态判断处于专注阶段还是休息阶段，并在一个阶段结束后自动进入下一阶段。每次完成专注后，会把对应的专注记录写入当天的文本文件，便于后续统计和日记联动。

正向计时模块适用于不固定时长的专注场景。用户结束计时后，系统将秒数换算成分钟并记录到本地文件中，与番茄计时形成统一的数据来源。

#### 2.5.2 日记记录模块

日记模块采用**按日期保存**的方式进行管理，也就是“一天一个文件”。这样做的好处是结构直观、方便查找、易于迁移。

除基本文本记录外，当前系统还支持：

- 字体和字号设置
- 加粗和斜体
- 图片插入
- 日期切换
- 语音输入扩展能力

因此，日记模块不仅承担记录文本的作用，也成为专注记录与个人生活信息的整合入口。

#### 2.5.3 记账管理模块

记账模块是本系统的重要组成部分。用户可以录入：

- 事项
- 金额
- 类型

系统使用表格展示账单记录，并支持：

- 新增记录
- 编辑记录
- 删除记录
- 类型扩展

底层数据结构为 `AccountingRecord`，采用JSON对象逐行写入的方式进行存储。这样既能保持结构化，又不会引入额外数据库依赖。

为了增强使用体验，我还实现了右键菜单交互，用户可以直接在表格中右键选择编辑或删除。同时，通过维护界面行与底层数据索引的映射，保证编辑和删除操作能够准确作用到目标记录。

#### 2.5.4 统计分析模块

统计模块主要分为两部分：

- 专注统计
- 消费统计

专注统计主要用于汇总周、月范围内的番茄计时和正向计时数据，计算专注总时长。

消费统计支持按日、周、月、年筛选账单数据，计算：

- 总额
- 平均值
- 最大值
- 最小值

在可视化方面，系统使用了自定义的饼状图和柱状图组件，通过Qt绘图机制完成渲染。饼图用于体现结构占比，柱状图用于体现数量对比。这样的设计既增强了系统展示效果，也体现了我对Qt绘图和界面交互的理解。

#### 2.5.5 图片处理与文字识别模块

为了进一步提升系统的扩展性，我在图片处理页面中加入了文字识别功能。

当前模块支持：

- 打开本地图片
- 基于四点选取进行透视调整
- 保存调整后的图像
- 对当前图片进行文字识别

在实现上，页面层并不直接耦合某个OCR引擎，而是通过统一的 `OcrService` 调用识别能力。当前项目中，这个服务层已经预留了多种技术路径：

- Paddle C++ Inference 本地推理
- PaddleOCR 脚本调用
- 其他OCR工具回退方案

基于这个统一接口，后续我计划正式接入**百度飞桨文字识别API**。这样做的好处是，不需要大规模改动页面层逻辑，就可以将识别后端升级为更稳定、更适合中文场景的OCR服务。未来这一能力还可以与记账模块联动，用于提取票据、截图和文档图片中的文本信息，从而辅助自动录入。

---

### 2.6 关键技术与实现特点

本项目的关键技术主要包括以下几个方面：

- **Qt Widgets**：用于构建桌面图形界面；
- **QTimer**：用于实现番茄计时与正向计时；
- **本地文件存储**：日记和专注记录使用文本存储，账单使用JSON逐行存储；
- **QPainter自定义绘图**：实现饼状图和柱状图；
- **QMenu 与动画交互**：优化记账模块的右键菜单体验；
- **QSS主题样式**：实现日间与夜间主题切换；
- **OCR服务抽象**：统一封装图片文字识别接口，为飞桨API接入做准备。

本项目的一个重要特点是：**不是简单叠加多个独立功能，而是强调功能之间的联动和整体体验的一致性**。例如专注记录可以进入日记与统计模块，图片识别能力未来可以服务于记账录入，主题切换可以统一作用于多个页面和控件。

---

### 2.7 测试与结果分析

在测试方面，我主要采用了**手工测试与自动化测试相结合**的方式。

手工测试主要用于检查：

- 页面布局是否合理
- 功能流程是否顺畅
- 图表展示是否正确
- 夜间主题是否统一

自动化测试主要基于 Qt 的 `QTest` 框架，对记账模块中的编辑、删除、数据刷新等关键逻辑进行验证。

从测试结果来看：

- 番茄计时和正向计时功能运行正常
- 日记模块能够正确按日期读写内容
- 记账模块能够完成增删改查
- 统计模块能够正确计算数据并刷新图表
- OCR模块能够完成图片加载、识别调用和结果展示
- 主题切换能够对主要界面元素生效

整体上，系统已经具备较好的可运行性和可展示性，能够满足毕业设计的功能需求。

---

### 2.8 创新点与特色

我认为本项目的特点和相对创新之处主要体现在以下几个方面：

1. **多功能整合**  
   将番茄计时、正向计时、日记、记账、统计和OCR扩展能力整合在同一桌面系统中。

2. **本地化与轻量化**  
   系统采用本地存储方案，减少部署成本，适合单机演示和个人使用。

3. **图形化统计展示**  
   利用自定义图表组件直观表达专注与消费数据。

4. **良好的扩展性**  
   通过服务层抽象OCR能力，为百度飞桨文字识别API等后续扩展预留统一入口。

5. **交互体验优化**  
   包括夜间主题、右键菜单动画、工具栏功能布局等细节优化。

---

### 2.9 存在的不足与改进方向

虽然系统已经实现了预期的大部分功能，但仍存在一些不足：

- 当前数据仍以本地文件存储为主，复杂查询能力有限；
- 登录与密码机制仍是本地演示级别，安全性不足；
- OCR模块当前完成了统一接口和本地识别路径预留，但百度飞桨API还处于待正式接入状态；
- 统计分析仍以基础汇总为主，缺少趋势预测和更深层次的数据挖掘。

后续我计划从以下几个方向进行改进：

- 引入SQLite提高查询效率；
- 增加多设备同步与数据备份能力；
- 正式接入百度飞桨文字识别API；
- 实现OCR识别结果与记账模块联动，辅助自动录入；
- 丰富统计分析维度，增强系统智能化水平。

---

### 2.10 答辩结束语

以上就是我的毕业设计答辩汇报内容。总体来说，我完成了一个基于Qt的综合性桌面应用系统，实现了时间管理、日记记录、记账分析以及图片文字识别扩展等功能，并围绕系统结构、数据组织、界面交互和后续扩展做了较为完整的设计与实现。

我的汇报到这里结束，感谢各位老师的聆听，恳请各位老师批评指正。

---

## 三、3分钟精简版发言稿

各位老师好，我的毕业设计题目是**《基于Qt的番茄计时与记账日记系统设计与实现》**。

本课题主要面向个人效率管理场景，目标是将番茄计时、正向计时、日记记录、记账管理、数据统计和图片文字识别整合到同一个桌面平台中，解决现有工具功能分散、使用割裂的问题。

系统采用C++和Qt Widgets开发，整体包含登录会话、番茄计时、正向计时、日记模块、记账模块、统计模块、主题管理以及图片处理与OCR扩展模块。数据方面，日记和专注记录使用文本文件存储，账单数据使用JSON逐行存储。统计模块使用自定义饼状图和柱状图完成数据展示。

在实现过程中，我重点完成了记账模块的增删改查、统计图表展示、夜间主题优化，以及图片处理页面中的文字识别能力设计。OCR部分通过统一的OcrService接口进行封装，为后续接入百度飞桨文字识别API预留了扩展能力。

经过测试，系统已能够稳定实现专注记录、日记保存、账单管理、统计分析和OCR结果展示等核心功能。后续我计划继续完善数据库存储、多端同步以及飞桨OCR正式接入等功能。

我的汇报完毕，谢谢各位老师。

---

## 四、答辩PPT讲解顺序建议

1. 课题背景与研究意义
2. 系统总体目标与功能模块
3. 系统架构设计
4. 番茄计时与日记模块实现
5. 记账模块与统计模块实现
6. 图片处理与OCR模块实现
7. 系统测试结果
8. 创新点、不足与展望

---

## 五、老师可能会问的答辩问题与参考回答

### 5.1 选题与背景类

#### 问题1：你为什么选择做这个题目？

**参考回答：**  
我选择这个题目主要有两个原因。第一，它贴近真实使用场景，时间管理、日记记录和记账管理都是个人日常中高频出现的需求。第二，这个课题能较好体现Qt桌面开发中的多个核心能力，包括界面设计、数据存储、模块协作、图表可视化以及后续OCR扩展，因此非常适合作为毕业设计课题。

#### 问题2：你的系统和现有软件相比，有什么特点？

**参考回答：**  
我这个系统的特点是功能集成度较高，把番茄计时、正向计时、日记、记账、统计和OCR扩展整合到了一个桌面端平台中。同时它采用本地化存储，部署简单，适合个人单机使用。另外，我在记账统计和夜间主题等细节交互上做了优化，并为百度飞桨OCR接入预留了统一服务接口。

---

### 5.2 技术选型类

#### 问题3：为什么选择Qt，而不是Electron或者Web技术？

**参考回答：**  
选择Qt主要是因为它在桌面应用开发中性能较好、跨平台能力强、控件丰富，而且和C++结合紧密，适合实现需要一定运行效率和界面控制能力的桌面系统。相比之下，Electron虽然开发方便，但资源占用较大；Web技术更适合浏览器场景，而我的课题更偏向本地桌面工具，因此Qt是更合适的选择。

#### 问题4：为什么没有一开始就使用数据库？

**参考回答：**  
因为当前系统定位是轻量级个人桌面工具，数据规模相对有限。采用本地文本和JSON存储能够降低开发复杂度，方便调试和演示，也更适合毕业设计当前阶段的实现目标。不过我在论文中也分析了这种方案的局限性，并提出后续可以引入SQLite来增强查询能力。

---

### 5.3 系统设计类

#### 问题5：你的系统是如何进行模块划分的？

**参考回答：**  
我主要按照职责进行划分，包括界面层、服务层和会话层。界面层负责交互和显示，服务层负责数据读写、统计和OCR识别调用，会话层负责维护登录状态。这样做可以降低耦合，方便后续功能扩展和维护。

#### 问题6：记账模块为什么要维护界面行和数据索引的映射？

**参考回答：**  
因为表格展示顺序和底层数据列表顺序在刷新后不一定完全一一对应，如果直接按当前表格行号操作，很容易出现编辑错位或删除错位的问题。因此我维护了界面行到数据索引的映射关系，确保右键菜单中的编辑和删除始终作用在正确的记录上。

---

### 5.4 功能实现类

#### 问题7：番茄计时模块的核心实现思路是什么？

**参考回答：**  
核心是利用QTimer每秒触发一次刷新，通过剩余秒数、当前轮次和是否处于休息状态三个关键状态量来控制流程。在一个阶段结束后，系统根据当前状态自动切换到休息或下一轮专注，并在专注完成后记录数据。

#### 问题8：为什么在记账模块中选择JSON逐行存储？

**参考回答：**  
因为账单数据具有天然的结构化特征，使用JSON可以比纯文本更清晰地表示字段关系，同时逐行存储也便于追加写入和逐条读取。对于当前项目规模来说，这种方案在可读性、实现成本和维护便利性之间取得了比较平衡的效果。

#### 问题9：统计模块为什么使用自定义图表，而不是直接用现成图表库？

**参考回答：**  
主要是出于两个考虑。第一，减少外部依赖，使项目结构更轻量；第二，自定义绘制更能体现我对Qt绘图流程和事件交互的掌握，也更方便和主题切换、点击事件等项目需求结合。

---

### 5.5 OCR与扩展类

#### 问题10：你为什么要在系统里加入OCR功能？

**参考回答：**  
因为OCR功能能够显著增强系统的扩展性和实用价值。例如，未来可以把票据、截图、图片中的文字提取出来，再与记账模块结合实现辅助录入。这样系统就不仅是“记录工具”，还具备了一定的信息提取和智能辅助能力。

#### 问题11：当前OCR功能实现到了什么程度？

**参考回答：**  
当前已经完成了图片处理页面、OCR调用入口和统一服务层抽象，支持本地图片加载、透视调整、识别调用和结果展示。同时，服务层已经预留了Paddle本地推理、脚本调用和回退方案。也就是说，整个结构已经具备，只差后续把百度飞桨文字识别API正式接入并优化结果处理。

#### 问题12：为什么选择百度飞桨文字识别API？

**参考回答：**  
因为飞桨OCR在中文场景下具有较好的适配能力，特别适合票据、截图、文档图片等应用场景。相比单纯依赖传统OCR方案，飞桨在准确率和可扩展性上更有优势。同时，我当前项目中已经预留了Paddle相关的服务封装，因此接入飞桨API在技术路线和架构设计上是自然延伸。

---

### 5.6 测试与不足类

#### 问题13：你是如何验证系统正确性的？

**参考回答：**  
我采用了手工测试和自动化测试相结合的方式。手工测试主要验证页面交互、主题切换和图表展示等内容；自动化测试主要基于QTest，对记账模块的编辑、删除和数据刷新等关键逻辑进行验证。这样可以兼顾界面体验和逻辑正确性。

#### 问题14：你的系统目前最大的不足是什么？

**参考回答：**  
我认为目前最大的不足有三个。第一，数据主要还是本地文件存储，复杂查询能力有限；第二，登录与密码机制还只是演示级别；第三，OCR模块虽然已经完成架构设计，但百度飞桨API还没有完全接入到正式业务流程中。这些也是我后续准备重点完善的方向。

#### 问题15：如果继续开发，你最优先做什么？

**参考回答：**  
如果继续开发，我会优先做两件事：第一是引入SQLite，提高账单和日记数据的管理能力；第二是完成百度飞桨文字识别API接入，并把OCR结果与记账模块打通，实现票据自动辅助录入。

---

## 六、老师追问时的高分表达模板

### 6.1 当老师问“你的创新点够不够”时

可以这样回答：

本项目的创新点不在于提出新的理论算法，而在于**面向真实使用场景进行综合系统设计与工程实现**。我将时间管理、日记记录、记账管理、数据统计和OCR扩展能力整合进一个Qt桌面系统，并围绕本地化存储、图表可视化、主题一致性、右键交互优化和OCR服务抽象做了较完整的设计。对于毕业设计来说，这种从需求分析到系统落地再到后续扩展的完整工程链条，本身就是很重要的价值体现。

### 6.2 当老师问“如果重做一次你会怎么改”时

可以这样回答：

如果重新做一次，我会在系统前期设计时更早引入数据层抽象，把日记、账单和OCR结果统一建模，这样后续扩展数据库和联动分析会更顺畅。同时，我会尽早设计OCR与记账的联动流程，减少后续功能追加时的接口调整成本。

### 6.3 当老师问“你的项目落地价值是什么”时

可以这样回答：

这个项目的落地价值主要体现在三个方面：第一，面向个人用户，具有直接的效率管理和信息整合价值；第二，面向工程实践，验证了Qt桌面系统在多模块综合应用中的可行性；第三，面向后续扩展，系统已经具备接入OCR、数据库和同步能力的基础，具有继续演化为完整产品原型的可能。

---

## 七、答辩前临场提醒

- 开场先报题目，再讲背景，不要一上来就讲代码
- 介绍功能时按照“问题—方案—效果”的顺序说
- 强调你做了什么，而不是只说系统有什么
- 老师追问时先肯定问题，再分析不足，最后给出改进方案
- OCR部分不要说“已经全部完成接入”，要说“已完成架构与入口设计，已具备接入百度飞桨API的技术基础”
- 如果老师问到创新性，不要硬说算法创新，要突出“系统整合、工程实现、扩展设计”

---

## 八、答辩一句话总结

本项目的核心价值在于：**基于Qt实现了一个集时间管理、日记记录、记账分析与图片文字识别扩展于一体的综合性桌面系统，并为后续接入百度飞桨OCR与智能化功能演进打下了良好的工程基础。**
