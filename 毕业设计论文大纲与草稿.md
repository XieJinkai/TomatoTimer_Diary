# 毕业设计（论文）

## 2.1.1 论文题目
**基于Qt的番茄计时与记账日记系统设计与实现**

## 2.1.2 摘要与关键词

### 2.1.2.1 中文摘要
面向大学生日常学习和个人生活管理场景，单一功能软件已经很难覆盖真实需求。我在项目开发过程中反复遇到一个很具体的问题：专注计时、日记记录和消费整理明明发生在同一天，却往往被分散在不同软件中，数据彼此割裂，回顾时也缺少连续性。围绕这一实际矛盾，本文设计并实现了一套基于Qt的番茄计时与记账日记系统。系统采用C++作为主要开发语言，使用Qt Widgets构建桌面界面，包含登录会话、番茄计时、正向计时、日记记录、记账管理、数据统计、主题切换以及图片处理与文字识别扩展等模块。数据层采用轻量化本地持久化方案，其中日记与专注记录以文本形式保存，账单数据以JSON对象逐行写入。实现过程中，系统利用QTimer完成计时逻辑，利用QTableWidget承担账单展示与编辑，结合QMenu和QPropertyAnimation实现右键菜单及动画交互，并通过自定义饼状图、柱状图展示消费结构和专注时长分布。针对图片文字提取需求，系统设计了统一的OcrService接口，为后续接入百度飞桨文字识别API或PaddleOCR推理能力预留扩展空间。测试结果表明，该系统在常见使用场景下运行平稳，界面响应正常，数据读写结果与预期一致，能够较好支持个人用户进行专注管理、日记留存、基础记账分析和图片文字提取。本文的工作更偏向工程实现，但我认为它也为轻量级桌面个人信息管理软件的设计提供了一条比较务实的实现路径。

### 2.1.2.2 中文关键词
Qt；C++；番茄工作法；记账系统；数据可视化；OCR文字识别

### 2.1.2.3 英文摘要
This thesis presents the design and implementation of a Qt-based desktop system that integrates Pomodoro timing, diary recording, accounting, and OCR-related utilities. The work originates from a practical observation made during development: focus records, daily notes, and expense data are often stored in separate tools, which weakens continuity and reduces the value of personal data review. To address this issue, the system is developed in C++ with Qt Widgets and organized into several modules, including session management, Pomodoro timer, forward timer, diary editing, accounting input, statistics, theme switching, image processing, and OCR extension support. A lightweight local persistence strategy is adopted. Diary and focus records are stored as text files, while accounting records are serialized line by line in JSON format. In implementation, QTimer is used for timing control, QTableWidget is used for bill presentation and editing, and QMenu with QPropertyAnimation is used to improve interaction details. In addition, custom pie charts and bar charts are used to visualize expense categories and focus duration statistics. For image text extraction, the system defines a unified OCR service abstraction so that PaddleOCR inference or the Baidu Paddle OCR API can be integrated in later stages. Functional testing indicates that the system remains responsive in common desktop scenarios and that its main data flows are correct and stable. The project is mainly an engineering-oriented undergraduate design, yet it also offers a practical reference for lightweight personal information management software on the desktop side.

### 2.1.2.4 英文关键词
Qt; C++; Pomodoro Technique; Accounting System; Data Visualization; OCR

## 2.1.3 目录
1. 绪论
    1.1 研究背景与意义
        1.1.1 研究背景
        1.1.2 研究意义
    1.2 国内外研究现状
        1.2.1 国外研究现状
        1.2.2 国内研究现状
        1.2.3 现有问题分析
    1.3 研究内容与论文结构
        1.3.1 研究内容
        1.3.2 论文结构安排
2. 系统需求分析与方案论证
    2.1 系统需求分析
        2.1.1 功能需求分析
        2.1.2 非功能需求分析
    2.2 关键技术与方案比较
        2.2.1 GUI框架选型
        2.2.2 数据存储方案比较
        2.2.3 图表实现方案比较
        2.2.4 OCR文字识别技术路线
    2.3 系统总体方案确定
        2.3.1 架构设计思路
        2.3.2 模块划分方案
3. 系统设计与实现
    3.1 系统总体架构设计
        3.1.1 系统模块组成
        3.1.2 页面关系与业务流程
    3.2 核心功能模块实现
        3.2.1 番茄计时与正向计时模块
        3.2.2 日记存储与记录模块
        3.2.3 记账录入与编辑删除模块
        3.2.4 图片处理与文字识别模块
    3.3 数据统计与界面交互实现
        3.3.1 消费统计与图表切换
        3.3.2 夜间主题与右键菜单交互
        3.3.3 本地数据持久化设计
4. 系统测试与结果分析
    4.1 测试环境与测试方法
        4.1.1 测试环境
        4.1.2 测试方法
    4.2 功能测试与结果分析
        4.2.1 计时与日记功能测试
        4.2.2 记账、统计与OCR功能测试
    4.3 系统性能与可用性分析
        4.3.1 响应性分析
        4.3.2 稳定性分析
5. 结论与展望
    5.1 研究工作总结
    5.2 存在的不足
    5.3 后续改进方向
致谢
参考文献
附录

## 2.1.4 正文

### 1 绪论

#### 1.1 研究背景与意义

##### 1.1.1 研究背景
在移动互联网高度普及之后，时间管理、日常记录和个人消费整理已经不再是小众需求，尤其在高校场景中更是如此。很多学生白天在教室、图书馆和宿舍之间切换，晚上再回头复盘时，会发现专注时长记在番茄钟软件里，情绪与事件写在备忘录里，支出又散落在另一个记账应用中。这种分散并不只是“麻烦”，它还会削弱数据的连续性。我在自己使用这类工具时就有明显感受：今天专注效率低，到底和课程安排、临时支出还是情绪波动有关，往往很难从多个孤立应用中直接看出来。

从产品供给看，现有工具并不算少。时间管理软件强调倒计时和提醒，记账软件更关注分类统计，日记软件则偏重文本编辑与云同步。它们各自成熟，却并未真正解决“同一用户、同一天、同一批行为数据”的整合问题。国内不少应用还存在广告较多、会员功能分层明显、数据导出受限等情况，这对只需要轻量记录和本地管理的用户并不友好。基于这样的现实背景，我尝试把番茄计时、日记和记账三类高频功能整合到一个Qt桌面系统中，并把图片文字识别作为扩展能力引入，目标不是做一个大而全的平台，而是完成一个结构清楚、可本地运行、适合个人长期使用的小型系统。

##### 1.1.2 研究意义
本课题的意义并不只在于“把几个功能放到一起”。从应用层面看，系统把专注记录、文字记录和消费数据纳入同一套本地管理路径中，用户回顾某一天的状态时，能够在一个界面中同时看到做了什么、花了多少钱、专注了多久。这种关联视角虽然看起来朴素，但对个人复盘是有价值的。以大学生日常学习为例，考试周、实习周和普通上课周的行为节奏差异很大，如果相关数据能够放在一起观察，很多变化会更直观。

从工程训练角度看，本项目覆盖了桌面软件开发中几类比较典型的问题：一方面需要扎实的C++语言基础与对象模型理解，特别是动态多态的实现机制、vtable/vptr等运行时结构，这部分在阅读资料和排查问题时给了我很直接的帮助[1][3][5][11]；另一方面，Qt桌面应用在工程组织上常见采用MVC或相近的分层方式来控制界面复杂度，我在模块拆分时也借鉴了这类思路[4][6]。除此之外，项目迭代过程中我使用Git管理分支与版本差异，避免“改着改着忘了怎么回退”的情况，这一点对毕业设计这种持续修改的项目尤其重要[2][15]。因此，本课题既具有一定的现实使用价值，也能较好体现本科阶段在桌面软件开发方向上的综合实践能力。

#### 1.2 国内外研究现状

##### 1.2.1 国外研究现状
国外关于时间管理和个人效率工具的研究与产品实践起步较早。以番茄工作法为代表的专注管理理念已经形成较稳定的应用场景，强调“短时专注—短时休息”的节奏控制[7]，相关软件通常会把倒计时、提醒、任务分组和阶段统计结合起来使用。部分桌面工具还加入跨设备同步、图形化复盘以及行为分析功能，使个人效率管理逐渐从“单次提醒”转向“长期追踪”。在桌面软件工程实现上，Qt的组件体系与事件机制较为成熟，配合MVC等框架化组织方式，能够在性能与界面控制之间取得较好的平衡[4][6]。

从国外软件工程实践来看，模块解耦和职责分层一直是桌面系统设计的重要方向。界面层、服务层和数据层分开组织，可以减少相互依赖带来的维护成本，这一点在不少经典设计实践中都得到强调[4][6]。我在阅读相关资料后发现，很多成熟系统并不是把“功能数量”作为唯一追求，而是更重视结构能否支撑后续扩展。这一观察对本项目影响很大，所以我没有把所有逻辑堆在窗口类中，而是尽量拆分为会话、存储、统计和识别等服务对象。

##### 1.2.2 国内研究现状
国内在时间管理、轻量记账和习惯打卡方面的产品发展很快，尤其是移动端应用生态已经较为丰富。用户能够很容易找到专门做番茄钟的App，也能找到主打消费分析、日程打卡或日记记录的软件。不过，桌面端产品的丰富程度明显不如移动端。很多同类工具要么是手机应用的附属版本，要么偏向商业办公场景，真正面向个人、强调本地独立运行的小型桌面系统并不多。

结合国内使用环境来看，这种现象并不难理解。移动端占据了大多数碎片化记录场景，而桌面端往往只在写作、学习和整理资料时才更有优势。可是在高校或个人办公环境里，桌面端也有它的实际价值：输入效率更高，信息展示面积更大，复杂表格编辑和多模块切换更顺手。尤其在宿舍、实验室或离线环境下，本地保存的数据更可控，迁移也直接。我在调研同类产品时就注意到，部分国产应用对网络和账户体系依赖较强，这会让“只想安静做记录”的用户产生额外负担。

##### 1.2.3 现有问题分析
综合国内外相关产品和开发思路，当前系统设计至少存在三类常见问题。其一，功能割裂较明显。专注、记账、日记往往分属不同应用，用户需要反复切换，数据也难以在同一语境下理解。其二，很多产品默认把云同步作为核心能力，但对本地隐私和离线使用的考虑不足。其三，桌面端工具在交互一致性、图表可读性和主题适配细节上，仍然有被忽视的地方。

我在实际使用和编码过程中还发现一个细节：很多软件即使提供统计，也只是对单一模块做汇总，很少把“专注时长”和“消费行为”或“日记内容”放到同一时间轴上理解。这样一来，数据虽然被记录下来了，却没有真正形成信息。基于此，本文将系统目标概括为“集成化、轻量化、本地化”。这里的“集成”不是功能堆砌，而是让专注记录、账单数据和日记文本共享同一套用户目录、同一套界面逻辑和同一套回顾视角。

#### 1.3 研究内容与论文结构

##### 1.3.1 研究内容
本文围绕一个基于Qt的桌面系统展开设计与实现，核心工作包括以下几个方面：构建登录与会话管理机制；完成番茄计时和正向计时功能；建立以日期为索引的日记记录流程；实现账单录入、展示、编辑和删除；编写消费统计与专注统计页面，并用自定义图表进行可视化表达；设计图片处理页面，加入透视校正与OCR接口调用入口；最后对系统主题切换、右键菜单和局部动画效果进行优化。

如果从开发过程看，这个项目并不是一次性成型的。我一开始更关注计时功能，后面逐渐意识到，只有加入记录和统计，系统才更像一个完整应用，而不是单独的练手页面。因此，本文也会呈现一个比较真实的演进过程：需求提出后，先完成可运行原型，再逐步补足数据持久化、交互细节和测试验证。研究方法上，本文主要采用需求分析、方案比较、模块实现和测试评估相结合的路径，并在实现阶段通过频繁迭代与回归验证来控制修改风险。

在具体研发组织上，我采用了Git进行版本控制，并通过TortoiseGit完成日常提交、分支切换与差异对比。实践中我更倾向于把关键方案放在独立分支上做验证：例如对图片文字识别模块，我曾分别验证“本地推理/本地脚本调用”和“云端WebAPI调用”的两条技术路线，再在对比稳定性、部署成本与用户体验后合并最终方案。对毕业设计而言，这种分支化试验既能降低试错成本，也能让论文中关于“方案比较—取舍—落地”的叙述更可追溯[2][15]。

##### 1.3.2 论文结构安排
全文共分为五章。第一章说明研究背景、研究意义以及相关现状，并解释本课题的切入点。第二章围绕系统需求和技术路线展开分析，对界面框架、数据存储、图表实现和OCR方案进行比较。第三章进入系统设计与实现部分，重点说明模块划分、业务流程以及关键功能的实现思路。第四章给出测试环境、测试方法和结果分析，既讨论功能正确性，也讨论响应性和可用性。第五章总结全文，并结合当前实现情况说明不足与后续改进方向。

### 2 系统需求分析与方案论证

#### 2.1 系统需求分析

##### 2.1.1 功能需求分析
结合项目当前的实现目标，系统至少需要覆盖几个稳定的使用场景。用户登录后应能进入统一主界面，并在不同功能页之间切换；番茄计时模块要支持任务名称输入、专注时长和休息时长设置，以及开始、暂停、继续和提前结束；正向计时模块要服务于时长不固定的专注场景，并把结果写回本地文件；日记模块应能按日期读取和保存内容；记账模块需要支持新增、查看、编辑、删除等基础操作；统计模块要能在周、月等范围内汇总数据并以图表形式展示；此外，系统还需要保留图片打开、透视校正和文字识别能力，方便后续处理票据、截图或文档图片。

这些需求并不是孤立成立的。以本项目为例，番茄计时和正向计时结束后产生的记录，不能只停留在某个页面变量里，而应进入本地文件，供日记和统计页面继续使用；记账数据录入后，也要在刷新时被统计模块重新读取；主题切换不能只改变主窗口背景，否则用户会立刻感到风格割裂。我在调试过程中就遇到过这种“不完全联动”的问题：一个页面颜色已经切到夜间模式，另一个弹出菜单仍然保持浅色，看起来很突兀。所以本系统的功能需求，实质上还包含了模块之间的协同要求。

##### 2.1.2 非功能需求分析
除功能本身外，系统还需要满足若干非功能要求。界面要尽量直观，避免用户在简单操作上花太多学习成本；程序在计时刷新、文件读写和图表重绘时应保持基本流畅，至少不能出现频繁卡顿；数据存储应以本地化为主，降低部署门槛；代码结构则要具备一定的可维护性，否则后续增加同步、数据库或更多工具页时会非常吃力。

我在实际编码阶段体会很深的一点是，本科毕设里的“可维护性”不是空话。如果早期把所有逻辑都写进主窗口类，后面任何一个功能修改都会牵连一大片代码。为此，系统把用户状态交由Session维护，把数据读写交给DataStore、DiaryStore和AccountingStore处理，把统计计算放进独立服务层，这种拆分方式更接近Qt应用中常见的MVC/分层组织思路，也能减少界面层与业务层之间的直接耦合[4][6]。当然，它也不是完全没有代价，类数量会变多，初学者读代码时会觉得路径绕了一点，但从长期修改的角度看，这样的代价是值得的。

#### 2.2 关键技术与方案比较

##### 2.2.1 GUI框架选型
桌面图形界面框架的选择直接影响开发节奏和最终效果。结合项目目标，我主要比较了Qt、Electron和WPF三种方案。Electron的前端生态丰富，上手速度快，但资源占用偏大；WPF在Windows平台的开发体验较成熟，不过跨平台能力有限；Qt则在组件成熟度与跨平台能力之间更均衡，且配套的应用组织方式相对成熟[4][6]。考虑到本项目是桌面端个人管理工具，而且后续可能有跨平台迁移需求，我最终选择Qt Widgets作为界面开发基础。

这个选择还有一个很现实的原因。Qt Widgets已经提供了QMainWindow、QTabWidget、QTableWidget、QMenu、QCalendarWidget等成熟控件，足以支撑本项目的大部分界面需求；同时在工程实践资料中也能找到较多可直接借鉴的实现套路，这对我这种边做边学的节奏很友好[6]。我不需要再额外引入过多第三方依赖，就可以完成主窗口组织、账单表格展示、日历切换和菜单交互等功能。再加上Qt的信号与槽机制较适合页面通信，登录成功、数据刷新、图表切换之类的事件可以被清楚地表达出来，这让我在开发阶段少走了一些弯路。

##### 2.2.2 数据存储方案比较
数据存储方案方面，理论上可以选择关系数据库、本地文本文件或JSON文件。对于单机、轻量级、数据规模不大的毕设项目来说，引入完整数据库会带来额外的部署与维护成本；相反，本地文本和JSON在落地实现、调试定位和数据迁移上更直接。用户拿到程序后无需单独配置数据库环境，开发者也可以直接打开文件查看记录内容，这一点在问题定位时非常方便。

基于这些考虑，系统最终采用混合本地存储策略：日记和专注记录以文本文件保存，便于按日期管理；记账数据以JSON对象逐行写入，保留结构化字段，同时降低解析复杂度。我在实现时也考虑过是否统一成一种格式，但实际试下来，日记更适合原样文本保存，账单则更适合结构化表达。这样的混合方案不算“最先进”，却更贴近当前项目规模。它的不足同样明显，例如复杂查询能力弱、并发控制缺失，因此后续如果要提升检索能力与数据一致性，引入嵌入式数据库会是更稳妥的方向。

##### 2.2.3 图表实现方案比较
统计页面需要图表支持，这部分可以直接接入第三方图表库，也可以基于Qt绘图机制自行实现。第三方库的优点是开发速度快、样式丰富，缺点是依赖增加，后续主题适配和交互细节未必完全受控。自定义绘制则更考验实现能力，但图形样式、响应区域和颜色逻辑都能按项目需要调整。综合考虑演示效果、课程训练价值以及可控性，本系统最终采用自定义饼状图和柱状图组件。

我之所以没有直接走“拿来即用”的方案，一个重要原因是统计图表在本项目里不只是展示图片，它还要和筛选条件、主题切换、点击交互共同工作。自定义实现虽然麻烦一些，但后期修改更加自由。比如夜间模式下图表边框、标签颜色和高亮扇区都要重新适配，这类需求如果完全依赖外部库，反而可能受限。对本科毕业设计来说，自定义绘制还能更真实地体现开发者对绘图流程和事件处理的理解，这一点在展示时也更有说服力。

##### 2.2.4 OCR文字识别技术路线
OCR功能属于扩展模块，但它并不是无关紧要的附属品。很多日常票据、截图和拍摄图片里的文字，手工录入确实费时，尤其在记账场景下更明显。从更大的技术图谱看，OCR属于机器视觉应用的一部分，往往需要配合图像预处理、畸变校正、版面分析等流程才能在真实场景下稳定工作[8]。围绕文字识别技术路线，当前可选方案大致包括传统OCR引擎、本地推理框架和云端OCR接口：本地推理自主可控，但环境配置和模型兼容性成本更高；云端API在部署复杂度上更低，便于快速接入到桌面系统中。在本项目里我将“透视校正+识别”的流程放在同一页面组织，透视变换部分主要依赖OpenCV完成，这是Qt+C++结合OpenCV进行视觉开发的常见路径之一[14]，也与一些基于OpenCV构建视觉平台与应用的实践经验相一致[9][12]。

在工程落地阶段，我先用本地PaddleOCR做过原型验证：优点是离线可用、调用链清晰；但在Windows CPU环境下，推理链对运行库、版本组合和硬件指令集的敏感性较强，实际集成时出现过兼容性问题，导致识别流程不够稳定。考虑到本系统定位为“轻量级桌面工具”，并且毕业设计需要可演示、易部署，我最终将默认识别路径切换为百度OCR WebAPI：该方案在接口层面更统一，依赖更少，部署与复现成本也更可控。为了保留后续扩展空间，系统仍通过统一服务接口保留本地识别与回退路径，便于未来在环境成熟时再切回本地推理或实现多后端自动选择。

我在本项目中没有把具体OCR实现写死在页面内部，而是先抽象出统一的OcrService接口。这样做的出发点很简单：当前阶段可以调用本地脚本或推理接口，后续如果需要接入云端WebAPI，只需要替换服务实现，而不必重写页面逻辑。对于桌面端Qt项目而言，这种把界面逻辑与服务调用拆开、按模块边界组织的方式更容易维护，也更贴近MVC类框架的工程实践[4][6]。同时，OCR只是视觉任务之一，类似光流用于人群运动估计等典型视觉算法也常通过“统一接口+可替换实现”的方式集成到平台中[13]，这一点也让我更坚定了接口抽象的做法。

#### 2.3 系统总体方案确定

##### 2.3.1 架构设计思路
在综合需求和技术比较结果后，系统整体采用模块化和分层化结合的思路。主窗口作为统一容器，通过标签页组织多个业务页面；界面层负责输入、展示和交互反馈；服务层负责数据读写、统计计算与OCR调用；会话层负责维护当前登录用户信息。这样的划分并不复杂，却能较好控制职责边界，减少不同模块之间的直接耦合，符合Qt项目常见的MVC式组织思路[4][6]。

从程序启动流程看，系统先创建QApplication对象并读取主题配置，再进入登录窗口。登录成功后，主窗口载入番茄钟、正向计时、统计、记账、日记和图片处理等页面。这个流程比较符合桌面软件的使用习惯，用户先完成身份进入，再处理具体业务。我在设计时有意避免“开机就把所有逻辑初始化到一起”的做法，因为那会让程序结构显得笨重，也不利于后期维护。

##### 2.3.2 模块划分方案
根据当前实现，系统可划分为登录会话模块、番茄计时模块、正向计时模块、日记模块、记账模块、数据统计模块、主题管理模块以及扩展工具模块。其中，记账、统计和图片文字识别模块使系统从单一计时工具延伸为综合型个人管理软件；而会话和主题模块虽然不直接产生业务数据，却影响系统的一致性和完整性。

模块之间通过相对清晰的接口协作。会话模块向各页面提供当前用户身份，存储模块负责组织用户目录与业务文件，统计模块按需调用存储服务汇总数据，主题管理模块统一加载样式表并应用到各类控件上。我在实际整理论文时发现，这种划分也让文字表达更容易落地，因为每个模块“做什么、不做什么”都比较明确，不至于写成一团含混的功能描述。

### 3 系统设计与实现

#### 3.1 系统总体架构设计

##### 3.1.1 系统模块组成
系统运行时首先进入登录窗口。用户完成登录后，程序切换至主窗口，主窗口以内嵌标签页方式组织番茄钟、正向计时、数据统计、记账、日记、图片处理等页面。这样设计的好处很直接：主要功能被放在同一工作区中，用户不需要在多个独立窗口之间来回寻找入口。对于个人工具软件来说，这种集中式界面通常比多窗口跳转更容易保持操作连续性。

主窗口采用QMainWindow作为顶层容器，内部通过QTabWidget完成页面切换，这是Qt Widgets中较为常见且稳定的组合方式。我在开发初期也考虑过把每个模块做成独立窗口，但测试后发现切换成本高，视觉上也容易零散。相较之下，统一容器更适合本项目的场景；在MVC式框架下以主窗口承载多个业务视图，也更容易统一管理会话与主题[4][6]。工具栏中提供主题切换动作，图片处理页和统计页则作为扩展能力保留在同一窗口中，既保证主业务集中，也给后续功能增加留出了空间。

##### 3.1.2 页面关系与业务流程
用户登录后，会话模块保存当前用户名，其余页面通过统一会话对象获取用户身份。番茄计时和正向计时结束时会将专注记录写入用户目录下的日期文本文件，日记模块负责相同日期内容的读取与维护；记账模块把账单对象写入独立数据文件；统计模块则读取专注记录和账单数据，按日、周、月等范围汇总并展示结果。整个流程形成了一个较完整的闭环，即“登录—记录—保存—统计—回顾”。

从程序结构上看，页面并不直接承担全部业务逻辑。界面类更多负责收集输入和反馈结果，而路径生成、文件读写、对象序列化和统计计算则下放到服务层中。以记账流程为例，用户在界面中输入事项、金额和类型后，页面只做必要校验和触发保存，真正的数据持久化由记账服务完成；统计页面刷新时也不会自己保存原始数据，而是从服务层重新读取。我在整理这一部分时特意保留了这些细节，因为它们能够说明项目不是单纯“把控件摆出来”，而是有一定层次设计的。

#### 3.2 核心功能模块实现

##### 3.2.1 番茄计时与正向计时模块
番茄计时模块以QTimer为基础，通过剩余秒数、当前轮次和休息状态等变量控制流程。用户可以自定义任务名称、专注时长、休息时长和番茄轮数，系统支持开始、暂停、继续和提前结束等操作。每轮专注结束后，程序自动把记录写入对应日期文件，为后续统计提供数据来源。正向计时模块则服务于更自由的专注场景，不预设固定轮次，结束后把秒级时长换算为分钟并保存。

这一部分实现看似简单，实际写起来有不少容易忽略的边角情况。比如用户中途提前结束时，如果完全丢弃当前时段，体验会比较差；但如果不做处理就直接记录，也可能造成统计失真。我的做法是根据已完成秒数进行折算，并在记录时做向上取整处理，让用户的有效专注成果不至于完全消失。这样的取舍未必是唯一正确答案，却比较符合桌面工具的使用直觉。也正因为有这些细节，计时模块和统计模块之间才能形成较稳定的数据衔接。

##### 3.2.2 日记存储与记录模块
日记模块采用按日期组织文件的思路，即“一天一文件”。系统通过统一的数据存储工具生成用户目录，再以“年-月-日.txt”的方式保存内容。这样做的优点是直观，用户甚至可以脱离程序，直接在文件系统中查看某天记录。对于毕设项目而言，这种方案虽然朴素，却很适合演示和调试，也更有利于后期备份和迁移。

除了基础文本保存外，日记页面还支持字体、字号、加粗、斜体等编辑操作，并允许插入本地图片。页面与日历选择组件配合后，用户可以较方便地切换日期查看内容。我在做这个模块时的感受是，日记功能如果只停留在一个空白文本框，其实很难体现系统价值；只有把日期管理、编辑能力和数据保存串起来，它才像一个真正可用的记录模块。项目中还预留了语音输入扩展入口，这部分目前并未完全展开，但保留了后续继续完善的可能。

##### 3.2.3 记账录入与编辑删除模块
记账模块是本项目中实现工作量较大、也是展示效果较明显的部分。页面提供事项、金额和类型三类输入项，账单记录通过表格集中展示。底层则使用独立的记账存储服务，把每条记录封装为包含时间、事项、金额和类型的结构体，再序列化为JSON文本逐行写入文件。这样处理后，记录既保留结构化特征，又便于后续读取和统计。

列表交互部分采用QTableWidget呈现时间、事项、金额和类型四列数据。为了避免刷新或排序后出现误操作，系统维护了界面行号与底层记录索引之间的映射关系。用户右键点击某条记录时，可弹出上下文菜单并执行编辑或删除。编辑时，原记录内容会回填到输入区域；删除时，系统先进行确认，再刷新表格并提示结果。我在调试这部分时最深的体会是，记账功能真正麻烦的地方不是“保存一条数据”，而是保证保存后还能准确编辑、删除，而且界面状态不能乱。

输入校验同样不能省略。事项不能为空，金额必须能被解析为有效数值，类型要来自已有选项或被追加保存，否则统计结果会很容易被脏数据干扰。除此之外，系统还把消费类型单独保存，以支持默认类型加载和自定义扩展。右键菜单的透明度动画虽然只是一个小细节，却明显改善了页面的完成度。严格说，它不属于核心算法，但我认为这类细微交互恰恰能体现桌面软件是否真的被认真打磨过。

##### 3.2.4 图片处理与文字识别模块
图片处理与文字识别模块位于扩展页面中，主要支持本地图片打开、透视校正、文字识别和结果保存。页面通过图像预览区域展示当前图片，并允许用户以四点选取的方式进行透视调整。这样做的原因很实际：拍照票据或课堂资料常常存在角度偏斜，若不先校正，后续识别效果通常会下降。

在实现策略上，界面层并不直接内嵌具体识别逻辑，而是通过OcrService统一发起识别请求。用户执行“文字识别”后，页面先把当前处理结果写入临时文件，再由服务层返回识别文本，最后显示到结果区域。这种设计让界面与识别后端解耦，也方便后续根据运行环境切换不同实现。考虑到机器视觉应用常常要在不同运行环境下取舍“本地推理/云端接口”，我在实现时保留了多种识别路径与回退策略，并把透视校正作为识别前的预处理步骤组织到同一流程中[8][14]。换句话说，当前模块已经有了“接口先行”的框架，虽然能力还未完全做满，但扩展方向是清晰的。

结合实际开发过程，OcrService在最终版本中默认优先走云端WebAPI识别，以降低本地环境差异对可用性的影响；当云端配置缺失或网络不可用时，再尝试本地脚本/本地推理等路径作为回退。这样安排并不追求“理论上最强”，而是更贴合毕业设计对稳定演示与可复现部署的要求：我更愿意把识别后端当作可替换部件，而把“界面交互—图片预处理—结果展示”的完整链路做扎实。

#### 3.3 数据统计与界面交互实现

##### 3.3.1 消费统计与图表切换
系统统计模块包含专注统计和消费统计两部分。专注统计主要面向周、月范围，汇总番茄事件数量、正向计时事件数量和专注总时长；消费统计则支持日、周、月、年四种范围筛选，并计算总支出、平均值、最大值和最小值。为了便于观察，系统分别设计了饼状图和柱状图，并通过视图切换方式在同一页面中展示不同图形。

专注统计和消费统计来源不同，但页面设计尽量保持一致，都采用“筛选条件+统计结果+图表展示”的组合。这样做的好处是用户切换模块时不必重新适应交互逻辑。对于消费统计，系统按类型聚合金额，突出不同类别支出的占比关系；对于专注统计，则更强调时间维度和累计投入。我在使用过程中会直观感觉到，自定义图表比单纯数字更容易帮助人理解自己的行为变化，不过它也不是没有问题，例如分类过多时标签会显得拥挤，这一点仍有优化空间。

##### 3.3.2 夜间主题与右键菜单交互
在界面交互优化方面，系统提供日间主题和夜间主题两种模式。主题切换并不是只改背景色，而是对主窗口、工具栏、标签页、按钮、输入框、下拉框、表格、菜单和日历等控件统一调整。夜间模式下，系统采用深色背景和较高对比度的文字、边框配色，以减轻弱光环境下的阅读压力。这个需求看似次要，但在桌面端连续使用时其实很容易被用户感知。

我在调试夜间主题时发现，真正困难的不是“提供一个深色样式表”，而是保证所有控件都跟着变，而且不会出现局部失真。例如图表颜色如果不重新适配，夜间模式下会显得过亮；菜单若仍保留浅底深字，也会很突兀。记账页面的右键菜单还加入了透明度动画，用QPropertyAnimation控制弹出和消失过程，这让操作节奏更自然。它不能决定系统成败，却能让软件少一点粗糙感。

##### 3.3.3 本地数据持久化设计
系统的数据持久化围绕本地用户目录展开。基础数据存储工具负责创建应用根目录和用户专属目录，日记与专注记录以文本方式保存，账单以JSON对象逐行写入，账单类型则单独存放。这样设计的直接好处是部署简单，程序可以在单机环境下独立运行，不依赖外部数据库，也不需要专门配置服务端。

从工程角度看，这种方案还具有一定容错性。某一类数据文件即便损坏，通常也只影响对应模块，不至于让整个系统全部失效。我个人比较认可这种“先保证能稳妥落盘，再考虑复杂能力”的思路，因为它更符合本科毕设从小到大、逐步扩展的开发节奏。当然，本地文件方案在复杂查询、并发访问和跨设备同步方面明显受限，这一点本文会在结论部分如实说明，而不是刻意回避。

### 4 系统测试与结果分析

#### 4.1 测试环境与测试方法

##### 4.1.1 测试环境
本系统主要在Windows环境下完成开发与测试，开发语言为C++，界面框架采用Qt Widgets，构建方式沿用Qt项目的常见流程。测试对象覆盖番茄计时、正向计时、日记读写、记账增删改查、统计图表渲染、主题切换以及OCR调用流程等主要功能。

由于项目定位为桌面本地应用，测试重点没有放在网络吞吐或高并发指标上，而是更关注交互是否顺畅、数据是否正确、模块间联动是否可靠。以我自己的测试经历来看，页面切换是否自然、夜间模式下文字是否清晰、右键菜单链路是否完整，这些问题虽然不算“高深”，却会直接影响软件的实际可用性，因此都被纳入测试范围。

##### 4.1.2 测试方法
测试方法采用手工测试与自动化测试相结合的方式。手工测试主要用于检查页面布局、交互流程和视觉效果，比如按钮逻辑是否符合预期、图表颜色是否协调、不同主题下控件是否可读。自动化测试则用于覆盖记账模块的关键逻辑，包括账单编辑、删除确认、数据刷新和提示信息反馈等内容。

这种组合方式比较适合当前项目。手工测试更容易发现“感觉不对”的细节，自动化测试则适合覆盖高频、重复且容易出错的业务路径。我在记账模块的修改过程中就明显体会到，如果没有自动化测试兜底，编辑和删除逻辑在后续改动中很容易被意外破坏。因此，虽然本项目测试规模不算大，但测试方法并不是形式化摆设，而是确实参与了功能稳定性的验证。

#### 4.2 功能测试与结果分析

##### 4.2.1 计时与日记功能测试
在计时模块测试中，系统能够正确完成开始、暂停、继续和提前结束等操作，计时结束后能把专注数据写入当日文件，并生成相应记录。日记模块能够按照日期正确加载和保存文本内容，文件路径与日期对应关系清楚，没有出现明显的错写、漏写问题。从测试结果看，计时和日记之间的数据联动是成立的，至少在日常使用场景中能够满足需求。

我还对多日期切换、文本编辑和图片插入进行了补充验证。结果表明，系统可以根据用户选择的日期读取正确文件，说明“一天一文件”的组织方式在当前规模下是稳定的。日记模块也不再只是一个简单文本框，而是具备了较基础的复合记录能力。当然，它离成熟笔记软件还有差距，例如格式控制和内容检索仍较弱，这一点在实际体验中是能感觉到的。

##### 4.2.2 记账、统计与OCR功能测试
记账功能测试主要围绕新增、查看、编辑和删除展开。结果显示，用户录入账单后，表格能够及时刷新；编辑操作可以正确回填原始数据，并在保存后写回文件；删除操作在确认取消时不会误删记录。统计模块在不同时间范围下可以正确计算总额、均值、最大值和最小值，并刷新饼状图和柱状图，说明数据读取、聚合和绘图这条链路是贯通的。图片处理与OCR模块则完成了本地图片加载、透视调整、识别调用和结果展示的基本流程，证明系统已具备继续接入正式OCR能力的结构基础。

为了使测试结果表达更清楚，本文将核心测试项整理如下表所示。

| 测试模块 | 测试内容 | 预期结果 | 实际结果 |
| --- | --- | --- | --- |
| 番茄计时 | 开始、暂停、继续、提前结束 | 状态切换正确，专注记录正常写入 | 通过 |
| 正向计时 | 计时结束并记录 | 能正确换算分钟并写入文件 | 通过 |
| 日记模块 | 按日期加载与保存 | 对应日期内容读写正确 | 通过 |
| 记账模块 | 新增账单 | 表格与文件数据同步更新 | 通过 |
| 记账模块 | 编辑账单 | 回填成功，修改后文件正确保存 | 通过 |
| 记账模块 | 删除账单 | 确认后删除，取消时不删除 | 通过 |
| 统计模块 | 图表切换与数据刷新 | 饼图、柱状图与统计值同步更新 | 通过 |
| OCR模块 | 图片加载与识别调用 | 能输出识别结果或明确提示 | 通过 |
| 主题模块 | 日夜间主题切换 | 各主要控件样式统一变化 | 通过 |

#### 4.3 系统性能与可用性分析

##### 4.3.1 响应性分析
从实际运行效果看，系统在普通个人电脑环境下启动较快，页面切换、按钮点击、图表切换和表格刷新都能在较短时间内完成。由于采用本地文件读写和轻量级Qt界面框架，系统在中小规模数据量下没有出现明显卡顿，基本符合桌面工具“点开即可使用”的预期。

尤其在记账和统计模块中，本地文件读取加内存汇总的方式，使系统在当前数据规模下保持了较好的响应速度。我在连续切换时间范围和图表视图时，没有感受到明显延迟。不过这种流畅性是建立在“数据量有限”的前提上的，如果后续账单规模大幅增长，或加入更复杂的全文检索与同步逻辑，现有方案的性能优势未必还能完全保持，这一点需要客观看待。

##### 4.3.2 稳定性分析
在持续使用和重复测试过程中，系统未出现明显崩溃、界面失效或严重数据异常。记账页面中的右键菜单、编辑状态切换和删除确认流程运行稳定，主题切换后多数控件也能保持正确显示，说明核心业务路径具备基本可靠性。对本科毕业设计来说，这种“能长期反复用而不轻易坏掉”的状态，比单次演示成功更有价值。

不过，系统稳定并不意味着没有边界。当前仍采用本地文件存储方案，在大规模数据、高并发访问和跨设备同步场景下会受到限制；OCR模块的实际识别效果也仍依赖后续具体后端实现。换句话说，当前系统更适合作为个人单机环境中的轻量应用原型，而不是直接面向复杂生产场景的成熟产品。我认为这样的定位是诚实的，也更符合本科毕业设计的实际要求。

### 5 结论与展望

#### 5.1 研究工作总结
本文围绕个人日常效率管理需求，完成了一套基于Qt的番茄计时与记账日记系统的设计与实现。系统将番茄计时、正向计时、日记记录、记账管理、统计分析以及图片文字识别扩展整合到同一桌面平台中，并通过本地文件持久化、自定义图表和主题切换等方式，形成了一个较完整的应用原型。

回看整个开发过程，我更愿意把这个项目理解为一次比较完整的工程训练，而不只是功能堆叠。项目从需求分析出发，经过方案比较、模块拆分、界面实现和测试验证，逐步形成了今天的结构。尤其是记账模块、统计图表和夜间主题的加入，让系统从单纯的计时工具逐渐变成了可持续使用的个人管理软件。这种演进过程本身，也构成了本文较有价值的一部分。

#### 5.2 存在的不足
尽管系统已经实现主要功能，但仍有几方面不足需要正视。其一，数据主要依赖本地文件存储，复杂查询能力有限，数据规模扩大后性能和组织方式都会受到挑战。其二，系统尚未真正完成多端同步和云备份，用户跨设备使用时仍然不够方便。其三，统计分析目前以基础汇总为主，对趋势预测、行为关联和自动建议等更深层能力支持不足。

除此之外，OCR模块在“工程形态”上仍有折中：当前默认采用云端WebAPI以保证稳定性与一致性，但这也带来了网络依赖、接口额度与密钥管理等问题；若在离线环境或严格隐私场景下使用，体验会受限。登录注册模块目前更偏向本地演示，密码处理和用户认证机制仍较简化；图表模块虽然可以完成展示和交互，但在标签密度控制、复杂图形支持和视觉细节上仍有提升空间。我在写完并重新回看这些功能后也能明显感觉到，系统已经足以支撑一篇本科毕业设计，但距离真正成熟的软件产品，还有一段不短的打磨过程。

#### 5.3 后续改进方向
后续改进可以沿着几个较明确的方向继续推进。数据层方面，可以引入嵌入式数据库以增强检索能力，并在数据规模扩大后更稳定地维护一致性。同步与共享方面，如果要实现跨设备或局域网数据交换，需要引入网络通信能力；在本科项目尺度下，先做基于Socket的同步原型更可控，工程门槛也相对低[10]。同时，为了避免网络请求、OCR识别等耗时操作影响界面响应，后续可以考虑用线程池统一调度后台任务，这类思路在Qt服务端/高并发场景的研究与应用中已有较多讨论[16]。OCR与图片处理方面，除了继续优化识别后端，图像预处理（如透视校正、增强、降噪）仍然是提升整体可用性的关键环节；Qt+C++结合OpenCV的实践经验对这部分优化具有直接参考意义[14]。版本迭代方面，随着功能增多，继续用Git规范分支和版本回退也能降低集成风险[2][15]。

如果从更长期的研究或产品化角度考虑，系统还可以进一步尝试用户行为分析、智能提醒、自动分类和OCR辅助录入。比如票据图片识别后，自动提取金额和类别并回填到记账页面；又或者结合一段时间内的专注记录和日记内容，给出相对个性化的复盘提示。我对这些方向保持兴趣，但也清楚它们已经超出了当前毕业设计的实现边界。先把基础系统做稳，再谈智能化扩展，可能更符合项目发展的实际节奏。

## 2.1.5 注释
注：番茄工作法是一种“固定专注时间与休息时间交替”的时间管理方法，常见配置为25分钟专注与5分钟休息，本文番茄计时模块的交互与统计口径参考该方法的基本思想[7]。  
注：JSON是一种轻量级结构化数据格式，适合用于账单对象的序列化与本地持久化保存。  
注：CRUD是创建（Create）、读取（Read）、更新（Update）、删除（Delete）四类基础数据操作的统称。  
注：OCR是光学字符识别（Optical Character Recognition）技术的简称，用于从图片、扫描件或票据中提取文本信息。  

## 2.1.6 参考文献
[1]董泉伶. 关于 c++动态多态性实现机制的探究[J]. 现代计算机(专业版)(11): 108-111.  
[2]Richard E. Silverman.Git Pocket Guide: A Working Introduction[M].O'Reilly Media,2012.  
[3]罗粮. C++中通过 vtable 实现多态性机制的分析[J]. 电脑编程技巧与维护, 2014(11): 3.  
[4]王玉亭, 孙剑. 基于mvc的Qt应用程序框架的设计与实现[J]. 计算机与数字工程, 2007(04): 179- 180.  
[5]普拉塔. C++ primer plus (第五版) 中文版. C++ Primer Plus (第五版) 中文版, 2005  
[6]陈志发, 王苑增. 嵌入式 Qt 实战教程, 2015.  
[7]史蒂夫・诺特伯格。番茄工作法图解：简单易行的时间管理方法 [M]. 大胖译。北京：人民邮电出版社，2011.  
[8]段峰, 王耀南, 雷晓峰, 等. 机器视觉技术及其应用综述[J]. 自动化博览, 2002, 19(3): 59-61  
[9]董鑫. 基于 OpenCV 的目视控制软件平台设计及实现[D]. 哈尔滨理工大学，2020.  
[10]李光明. 应用 SOCKET 实现网络通信[J]. 通信技术, 2012, 31(8):156-157  
[11]ISO/IEC 14882: 2011(E), Programming languages- C++.  
[12]于媛. 基于 OpenCV 的校园智能视频监控设计与实现[D]. 天津工业大学, 2017  
[13]Chen H, Ye S, Nedzvedz O V, et al. Application of integral optical flow for determining crowd movement from video images obtained using video surveillance systems[J]. Journal of Applied Spectroscopy, 2018, 85(1): 126-133  
[14]朱文伟，李建英.OpenCV 计算机视觉开发实践：基于 Qt C++[M]. 清华大学出版社，2025.  
[15]Jon Loeliger,Matthew McCullough.Version Control with Git[M].O'Reilly Media,2012.  
[16]刘飞, 张卫强, 罗彤. 基于 Qt 的线程池服务器的研究与应用[J]. 无线电通信技术, 2014, 40(6): 5.  

## 2.1.7 致谢
在本次毕业设计与论文撰写过程中，我得到了许多帮助。指导教师在选题、结构调整、系统实现和论文修改等方面给予了耐心指导，让我在遇到问题时不至于停在原地。几位同学也在功能体验、测试反馈和细节讨论中提供了不少直接意见，很多看似细小的问题，正是在反复交流后才被我注意到。家人对我完成毕业设计期间的学习安排和情绪波动始终保持理解，这种支持其实非常重要。在此，我向所有给予帮助的老师、同学和家人表示诚挚谢意。

## 2.1.8 附录

### 附录A 记账模块核心数据结构

```cpp
struct AccountingRecord {
    QDateTime time{};
    QString item{};
    double amount{0.0};
    QString type{};
};
```

### 附录B 系统主要模块说明
（1）番茄计时模块：负责专注倒计时、轮次切换、暂停继续与记录写入。  
（2）正向计时模块：负责普通专注事件计时与分钟统计。  
（3）日记模块：负责按日期保存和读取文本内容。  
（4）记账模块：负责账单录入、表格展示、右键编辑与删除。  
（5）统计模块：负责专注数据与消费数据的汇总和图表展示。  
（6）图片处理与OCR模块：负责图片打开、透视调整、文字识别与结果展示。  
（7）主题管理模块：负责日间主题、夜间主题样式加载与保存。  
（8）登录会话模块：负责用户状态维护和页面访问前提控制。  

### 附录C 建议插图位置
（1）系统登录界面与主窗口界面图。  
（2）番茄计时页面运行效果图。  
（3）记账页面新增、编辑和删除操作效果图。  
（4）统计页面饼状图、柱状图及夜间主题效果图。  
（5）图片处理页面与文字识别结果展示效果图。  
