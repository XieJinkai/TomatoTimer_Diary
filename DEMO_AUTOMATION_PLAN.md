# TomatoTimer_Diary 演示脚本确认稿

## 目标

生成一个“演示用自动脚本”，运行后按固定顺序展示项目核心功能，适合答辩、汇报或录屏演示。

本确认稿只定义演示内容与节奏，暂不生成脚本。

## 演示原则

1. 演示以“老师/观众第一次接触系统”为前提，顺序从登录进入，到核心功能，再到统计收尾。
2. 每一步都尽量可见、可理解，不做太快的跳转。
3. 演示脚本只负责“展示功能”，不依赖人工临场点按。
4. 演示数据尽量由脚本自动准备，避免现场因为空数据导致界面不好看。
5. 所有演示都以当前本地构建出的程序为对象。

## 计划演示的功能

### 1. 启动与登录

- 启动程序
- 展示登录/注册界面
- 自动填写演示账号
- 如账号不存在则自动注册
- 自动登录进入主界面

演示目的：
让观众先看到系统入口，说明本系统具备基础的本地用户管理能力。

### 2. 番茄钟页面

- 进入“番茄钟”页签
- 展示番茄数、专注时长、休息时长等输入项
- 自动填写一组演示参数
- 点击开始
- 适当停留，展示计时界面确实开始工作
- 为避免等待过久，演示脚本可提前结束当前番茄钟

演示目的：
说明这是一个围绕效率管理设计的桌面应用，而不是单纯的记账工具。

### 3. 正向计时页面

- 切换到“正向计时”页签
- 点击开始
- 停留几秒
- 点击结束
- 如页面支持备注，则填写一条演示备注

演示目的：
展示除番茄钟外，系统也支持自由计时场景。

### 4. 记账页面

- 切换到“记账”页签
- 依次录入数条演示消费记录
- 覆盖至少 3 个分类
- 展示记录表格刷新结果
- 演示一次分类筛选
- 演示一次关键词搜索

建议演示数据：

- 午餐，28.50，餐饮
- 地铁，4.00，交通
- 书籍，56.00，学习
- 咖啡，18.00，餐饮
- 生活用品，23.90，购物

演示目的：
这是当前项目的重要补充模块，需要明确展示“已可用”，并让观众看到录入、筛选、展示三部分能力。

### 5. 日记页面

- 切换到“日记”页签
- 打开当天日记
- 自动写入一段演示文本
- 执行保存

建议文案：

“今天完成了系统功能演示，包括番茄钟、正向计时、记账与统计模块，验证了核心流程可正常运行。”

演示目的：
体现项目“番茄计时 + 记账 + 日记”的组合定位。

### 6. 数据统计页面

- 切换到“数据统计”页签
- 展示专注统计区域
- 展示消费统计区域
- 如果支持图表切换，则自动点一次“切换图表”
- 在消费统计中展示已有分类汇总结果

演示目的：
作为演示收尾，向观众展示系统不只是记录数据，还能做聚合展示。

### 7. 设置/同步页面

- 切换到“设置/同步”页签
- 停留数秒用于展示界面

演示目的：
完整展示系统模块结构，避免观众误以为该页面未实现。

## 推荐演示顺序

1. 启动与登录
2. 番茄钟
3. 正向计时
4. 记账
5. 日记
6. 数据统计
7. 设置/同步

## 脚本执行方式建议

建议最终脚本具备以下行为：

- 自动启动 `build/Debug/tomato_timer.exe`
- 自动识别窗口
- 自动填写输入框并点击按钮
- 在关键步骤间加入 1 到 3 秒停顿
- 在演示结束后保留程序界面，不立即关闭

## 解说稿风格建议

后续解说稿建议采用“答辩式讲解”，每一步只讲当前页面最重要的价值，不念代码，也不读按钮名。

目标风格：

- 简洁
- 正式
- 适合 3 到 5 分钟演示

## 暂不包含的内容

这版自动演示暂不默认包含以下能力，除非你确认要加：

- 图片处理模块的复杂操作演示
- OCR 或语音相关功能演示
- 文件选择器弹窗类操作
- 多账号切换演示
- 云同步真实目录复制演示

## 我接下来会做的内容

如果你确认这份文档，我下一步会生成两部分：

1. 一个可直接运行的演示脚本
2. 一份配套中文解说稿

## 请你确认的点

请重点确认以下内容是否符合你的预期：

- 演示顺序是否合适
- 是否要保留“番茄钟”和“正向计时”演示
- 记账模块是否要作为重点展示模块
- 是否需要把图片处理也加入自动演示
- 解说稿是要“答辩风格”还是“产品展示风格”
